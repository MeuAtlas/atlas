"use client";
import { Area,AreaChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";
import type { FinancialTransaction } from "@/modules/finance/types";
export function FinanceChart({transactions}:{transactions:FinancialTransaction[]}){const grouped=new Map<string,{date:string;receitas:number;despesas:number}>();for(const t of transactions.filter(t=>t.status==="realized"&&t.transaction_type!=="transfer").slice().reverse()){const key=t.competence_date.slice(5);const row=grouped.get(key)??{date:key,receitas:0,despesas:0};if(["income","refund","reversal"].includes(t.transaction_type))row.receitas+=Number(t.amount);if(t.transaction_type==="expense")row.despesas+=Number(t.amount);grouped.set(key,row)}const data=[...grouped.values()].slice(-12);if(!data.length)return <div className="finance-chart-empty">O gráfico aparecerá após sua primeira movimentação realizada.</div>;return <div className="finance-chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data}><defs><linearGradient id="income" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#37d6bd" stopOpacity={.45}/><stop offset="95%" stopColor="#37d6bd" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="rgba(148,163,184,.1)" vertical={false}/><XAxis dataKey="date" stroke="#8492a9"/><YAxis hide/><Tooltip formatter={(v)=>`R$ ${Number(v).toLocaleString("pt-BR")}`}/><Area type="monotone" dataKey="receitas" stroke="#37d6bd" fill="url(#income)"/><Area type="monotone" dataKey="despesas" stroke="#ff718b" fill="transparent"/></AreaChart></ResponsiveContainer></div>}
