import assert from "node:assert/strict";
import test from "node:test";
import { closureId, materializeClosureLines, summarizeClosure } from "./financial-closure";
test("keeps rate and decision unknown instead of converting them to zero", () => { const lines = materializeClosureLines("closure", [{ componentType: "NORMAL_VARIABLE", sourceImport: "MIXED", quantity: 3600, quantityUnit: "SECONDS", rateMinorUnits: null, calculationStatus: "UNKNOWN", reason: "DECISION_UNKNOWN", currency: "BRL", parameterId: null, confidence: "LOW" }]); assert.equal(lines[0].amountMinorUnits, null); });
test("uses cents and applies seniority only to eligible floor", () => { const lines = materializeClosureLines("closure", [{ componentType: "SENIORITY", sourceImport: "NONE", quantity: 10000, quantityUnit: "CENT", rateMinorUnits: 1, multiplierNumerator: 7, multiplierDenominator: 100, calculationStatus: "CALCULATED", reason: null, currency: "BRL", parameterId: "FLOOR", confidence: "HIGH" }]); assert.equal(lines[0].amountMinorUnits, 700); });
test("keeps the documented 7 percent seniority calculation exact in cents", () => { const lines = materializeClosureLines("closure", [{ componentType: "SENIORITY", sourceImport: "NONE", quantity: 902592, quantityUnit: "CENT", rateMinorUnits: 1, multiplierNumerator: 700, multiplierDenominator: 10000, calculationStatus: "CALCULATED", reason: null, currency: "BRL", parameterId: "FLOOR", confidence: "HIGH" }]); assert.equal(lines[0].amountMinorUnits, 63181); });
test("keeps mixed currencies out of a combined total and reports incomplete", () => { const lines = materializeClosureLines("closure", [{ componentType: "DOMESTIC_LUNCH", sourceImport: "EXECUTED", quantity: 1, quantityUnit: "ENTITLEMENT", rateMinorUnits: 10995, calculationStatus: "CALCULATED", reason: null, currency: "BRL", parameterId: "MEAL", confidence: "HIGH" }, { componentType: "INTERNATIONAL_DIEMS", sourceImport: "EXECUTED", quantity: 1, quantityUnit: "ENTITLEMENT", rateMinorUnits: null, calculationStatus: "UNKNOWN", reason: "CATALOG_MISSING", currency: null, parameterId: null, confidence: "LOW" }, { componentType: "SUNDAY", sourceImport: "MIXED", quantity: 1, quantityUnit: "SECONDS", rateMinorUnits: null, calculationStatus: "UNKNOWN", reason: "DECISION_UNKNOWN", currency: "BRL", parameterId: null, confidence: "LOW" }]); const summary = summarizeClosure(lines); assert.equal(summary.knownTotalBrl, 10995); assert.equal(summary.finalTotalBrl, null); assert.equal(summary.finalTotalStatus, "INCOMPLETE"); });
test("uses stable closure and line identities", () => { assert.equal(closureId("user", 2026, 8, "import"), closureId("user", 2026, 8, "import")); });
