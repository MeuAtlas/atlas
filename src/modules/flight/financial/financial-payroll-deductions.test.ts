import assert from "node:assert/strict"; import test from "node:test"; import { calculateInss,calculateIrrf,valuePersonalDeduction } from "./financial-payroll-deductions";
test("INSS 2026 is progressive and capped",()=>{assert.equal(calculateInss(162100).amountCents,12158);assert.equal(calculateInss(9999999).amountCents,calculateInss(847555).amountCents);});
test("IRRF chooses legal deductions and subtracts alimony once from the tax base",()=>{const pension={id:"p",name:"Pensão",calculationType:"PERCENTAGE_MINIMUM_WAGE" as const,amountMinorUnits:null,percentageBasisPoints:15000,deductibleFromIrrfBase:true};assert.equal(valuePersonalDeduction(pension),243150);const tax=calculateIrrf(2200000,calculateInss(2200000).amountCents,[pension]);assert.equal(tax.method,"LEGAL_DEDUCTIONS");assert.equal(tax.baseCents,2200000-tax.legalDeductionsCents);});
test("simplified deduction wins when legal deductions are smaller",()=>assert.equal(calculateIrrf(300000,10000,[]).method,"SIMPLIFIED"));
